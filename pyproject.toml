[project]
name = "pi-subagent"
version = "0.1.0"
description = "Launch isolated Pi subagents and retain their event logs"
requires-python = ">=3.11"

[project.scripts]
pi-subagent = "pi_subagent:main"

[build-system]
requires = ["uv_build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
